# .github/workflows/deploy.yml
name: Deploy Website

on:
  push:
    branches:
      - main
    # Ignore data.json changes to prevent redundant builds
    paths-ignore:
      - 'data.json'
      - '**.md'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and Deploy
        run: |
          # Your existing build/deploy commands here
          echo "Building site..."
